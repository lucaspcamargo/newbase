A classic platformer demo showcasing tilemap loading and rendering via the **Tiled JSON** format,
tileset support with spacing/margin, and Box2D collision from tile layers.

Use **arrow keys** to move and **[Space]** to jump.
