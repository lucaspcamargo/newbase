#pragma once

// {{outname}}
// RTTI initialization entry point
// Generated by {{progname}} at {{datetime}}

#include <stdio.h>

{% for id in identifiers %}
void _rtti_init_{{id}}();
{% endfor %}

void _rtti_init_newbase()
{
    fprintf(stderr, "rtti init...");
    {% for id in identifiers %}
    _rtti_init_{{id}}();
    {% endfor %}
    fprintf(stderr, "rtti init DONE");
}
