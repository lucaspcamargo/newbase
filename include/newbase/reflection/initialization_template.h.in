#pragma once

// {{outname}}
// RTTI initialization entry point
// Generated by {{progname}} at {{datetime}}
// To be called once from C as first step of executable initialization

#include <stdio.h>

void _rtti_init_engine();
{% for id in identifiers %}
void _rtti_init_{{id}}();
{% endfor %}

void _rtti_init_newbase()
{
    fprintf(stdout, "[rtti] init...\n");

    _rtti_init_engine();
    {% for id in identifiers %}
    _rtti_init_{{id}}();
    {% endfor %}
    
    fprintf(stdout, "[rtti] init OK\n");
}
