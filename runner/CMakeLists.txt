# this is meant to be a complete binary with all systems,
# used to run game archives/folders with no fuss
# this is only worth doing when the engine is more mature
# for now all engine development happens with the demo